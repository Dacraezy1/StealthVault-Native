cmake_minimum_required(VERSION 3.22.1)

project("stealthvaultnative")

add_library(
        stealthvault
        SHARED
        native-lib.cpp)

find_library(
        log-lib
        log)

target_link_libraries(
        stealthvault
        ${log-lib})
